<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SeguridadInformatica.MVVM.Views.Strangers1"
             Title="No Hables con Extraños en Línea">
    <ScrollView>
        <VerticalStackLayout Padding="15" Spacing="10">
            <Label Text="No Compartir Información Personal"
                   FontSize="24"
                   FontAttributes="Bold"
                   VerticalOptions="Start"
                   HorizontalOptions="Center" />

            <Image Source="strange.jpg"
                   HorizontalOptions="Center"
                   Aspect="AspectFit"
                   HeightRequest="200" />

            <Label Text="Puede llevar a robos de identidad, donde los delincuentes utilizan tu información para hacerse pasar por ti y cometer fraudes. Además, compartir datos personales puede exponer tu privacidad, permitiendo que personas no autorizadas accedan a detalles sensibles sobre tu vida. También puede aumentar el riesgo de ciberacoso y extorsión."
                   FontSize="16"
                   LineBreakMode="WordWrap"
                   VerticalOptions="StartAndExpand" />

            <Label Text="Que Hemos Aprendido"
                   FontSize="24"
                   FontAttributes="Bold"
                   VerticalOptions="Start"
                   HorizontalOptions="Center" />

            <Label StyleClass="LightBlue,SubHeader" Text="¿Por qué no se debe compartir información personal en línea?" />

            <VerticalStackLayout Spacing="10">
                <HorizontalStackLayout Spacing="10">
                    <CheckBox x:Name="checkBoxPractica1" />
                    <Label Text="Para proteger tu privacidad y evitar el robo de identidad."
                           VerticalOptions="Center" />
                </HorizontalStackLayout>

                <HorizontalStackLayout Spacing="10">
                    <CheckBox x:Name="checkBoxPractica2" />
                    <Label Text="Para que más personas puedan conocerte mejor."
                           VerticalOptions="Center" />
                </HorizontalStackLayout>
            </VerticalStackLayout>

            <Button Text="Terminar"
                    VerticalOptions="End"
                    HorizontalOptions="Center"
                    Clicked="OnEvaluateAnswerClicked" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>